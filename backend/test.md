# 📘 Руководство по тестированию FastAPI Shop API в Postman

## 🎯 Подготовка к тестированию

### 1. Запуск бэкенда

```bash
# Перейдите в директорию backend
cd backend

# Установите зависимости (если еще не установлены)
pip install -r requirements.txt

# Заполните базу данных тестовыми данными
python seed_data.py

# Запустите сервер
python run.py
```

Сервер запустится на `http://localhost:8000`

### 2. Проверка доступности API

Откройте в браузере: `http://localhost:8000/api/docs` - вы должны увидеть автоматическую документацию Swagger.

---

## 📋 Тестовые сценарии

### Сценарий 1: Базовая проверка API

#### ✅ Тест 1.1: Проверка корневого эндпоинта

**Запрос:**
- **Метод:** GET
- **URL:** `http://localhost:8000/`

**Ожидаемый результат:**
```json
{
  "message": "Welcome to fastapi shop API",
  "docs": "api/docs"
}
```

**Статус код:** 200 OK

---

### Сценарий 2: Работа с категориями

#### ✅ Тест 2.1: Получение всех категорий

**Запрос:**
- **Метод:** GET
- **URL:** `http://localhost:8000/api/categories`

**Ожидаемый результат:**
- Массив категорий с полями: `id`, `name`, `slug`
- Должно быть 4 категории: Electronics, Clothing, Books, Home & Garden

**Статус код:** 200 OK

**Пример ответа:**
```json
[
  {
    "id": 1,
    "name": "Electronics",
    "slug": "electronics"
  },
  ...
]
```

#### ✅ Тест 2.2: Получение категории по ID

**Запрос:**
- **Метод:** GET
- **URL:** `http://localhost:8000/api/categories/1`

**Ожидаемый результат:**
- Одна категория с `id: 1` (Electronics)

**Статус код:** 200 OK

#### ❌ Тест 2.3: Получение несуществующей категории

**Запрос:**
- **Метод:** GET
- **URL:** `http://localhost:8000/api/categories/9999`

**Ожидаемый результат:**
```json
{
  "detail": "Category with id 9999 not found"
}
```

**Статус код:** 404 NOT FOUND

---

### Сценарий 3: Работа с товарами

#### ✅ Тест 3.1: Получение всех товаров

**Запрос:**
- **Метод:** GET
- **URL:** `http://localhost:8000/api/products`

**Ожидаемый результат:**
- Объект с полями `products` (массив) и `total` (количество)
- В каждом товаре должна быть информация о категории

**Статус код:** 200 OK

**Пример ответа:**
```json
{
  "products": [
    {
      "id": 1,
      "name": "Wireless Headphones",
      "description": "High-quality wireless headphones...",
      "price": 299.99,
      "category_id": 1,
      "image_url": "https://images.unsplash.com/...",
      "created_at": "2024-01-20T10:30:00",
      "category": {
        "id": 1,
        "name": "Electronics",
        "slug": "electronics"
      }
    }
  ],
  "total": 13
}
```

#### ✅ Тест 3.2: Получение товара по ID

**Запрос:**
- **Метод:** GET
- **URL:** `http://localhost:8000/api/products/1`

**Ожидаемый результат:**
- Один товар с полной информацией включая категорию

**Статус код:** 200 OK

#### ✅ Тест 3.3: Получение товаров по категории

**Запрос:**
- **Метод:** GET
- **URL:** `http://localhost:8000/api/products/category/1`

**Ожидаемый результат:**
- Только товары категории Electronics (category_id: 1)
- Должно быть 5 товаров

**Статус код:** 200 OK

#### ❌ Тест 3.4: Получение несуществующего товара

**Запрос:**
- **Метод:** GET
- **URL:** `http://localhost:8000/api/products/9999`

**Ожидаемый результат:**
```json
{
  "detail": "Product with id 9999 not found"
}
```

**Статус код:** 404 NOT FOUND

---

### Сценарий 4: Работа с корзиной

#### ✅ Тест 4.1: Добавление товара в пустую корзину

**Запрос:**
- **Метод:** POST
- **URL:** `http://localhost:8000/api/cart/add`
- **Headers:** `Content-Type: application/json`
- **Body:**
```json
{
  "product_id": 1,
  "quantity": 2,
  "cart": {}
}
```

**Ожидаемый результат:**
```json
{
  "cart": {
    "1": 2
  }
}
```

**Статус код:** 200 OK

**Что проверить:**
- Товар добавлен с правильным количеством
- Структура ответа соответствует ожиданиям

#### ✅ Тест 4.2: Добавление еще одного товара в корзину

**Запрос:**
- **Метод:** POST
- **URL:** `http://localhost:8000/api/cart/add`
- **Body:**
```json
{
  "product_id": 2,
  "quantity": 1,
  "cart": {
    "1": 2
  }
}
```

**Ожидаемый результат:**
```json
{
  "cart": {
    "1": 2,
    "2": 1
  }
}
```

#### ✅ Тест 4.3: Увеличение количества существующего товара

**Запрос:**
- **Метод:** POST
- **URL:** `http://localhost:8000/api/cart/add`
- **Body:**
```json
{
  "product_id": 1,
  "quantity": 3,
  "cart": {
    "1": 2
  }
}
```

**Ожидаемый результат:**
```json
{
  "cart": {
    "1": 5
  }
}
```

**Примечание:** Количество суммируется (2 + 3 = 5)

#### ✅ Тест 4.4: Получение детальной информации о корзине

**Запрос:**
- **Метод:** POST
- **URL:** `http://localhost:8000/api/cart`
- **Body:**
```json
{
  "1": 2,
  "2": 1,
  "3": 3
}
```

**Ожидаемый результат:**
```json
{
  "items": [
    {
      "product_id": 1,
      "name": "Wireless Headphones",
      "price": 299.99,
      "quantity": 2,
      "subtotal": 599.98,
      "image_url": "https://..."
    },
    {
      "product_id": 2,
      "name": "Smart Watch Pro",
      "price": 399.99,
      "quantity": 1,
      "subtotal": 399.99,
      "image_url": "https://..."
    },
    {
      "product_id": 3,
      "name": "Laptop Stand",
      "price": 49.99,
      "quantity": 3,
      "subtotal": 149.97,
      "image_url": "https://..."
    }
  ],
  "total": 1150,
  "items_count": 6
}
```

**Что проверить:**
- Правильность подсчета subtotal для каждого товара
- Правильность итоговой суммы
- Правильность общего количества товаров

#### ✅ Тест 4.5: Получение пустой корзины

**Запрос:**
- **Метод:** POST
- **URL:** `http://localhost:8000/api/cart`
- **Body:**
```json
{}
```

**Ожидаемый результат:**
```json
{
  "items": [],
  "total": 0.0,
  "items_count": 0
}
```

#### ✅ Тест 4.6: Обновление количества товара в корзине

**Запрос:**
- **Метод:** PUT
- **URL:** `http://localhost:8000/api/cart/update`
- **Body:**
```json
{
  "product_id": 1,
  "quantity": 5,
  "cart": {
    "1": 2,
    "2": 1
  }
}
```

**Ожидаемый результат:**
```json
{
  "cart": {
    "1": 5,
    "2": 1
  }
}
```

**Примечание:** Количество заменяется, а не суммируется

#### ✅ Тест 4.7: Удаление товара из корзины

**Запрос:**
- **Метод:** DELETE
- **URL:** `http://localhost:8000/api/cart/remove/2`
- **Body:**
```json
{
  "cart": {
    "1": 2,
    "2": 1,
    "3": 3
  }
}
```

**Ожидаемый результат:**
```json
{
  "cart": {
    "1": 2,
    "3": 3
  }
}
```

**Что проверить:**
- Товар с `product_id: 2` удален
- Остальные товары остались без изменений

#### ❌ Тест 4.8: Добавление несуществующего товара

**Запрос:**
- **Метод:** POST
- **URL:** `http://localhost:8000/api/cart/add`
- **Body:**
```json
{
  "product_id": 9999,
  "quantity": 1,
  "cart": {}
}
```

**Ожидаемый результат:**
```json
{
  "detail": "Product with id 9999 not found"
}
```

**Статус код:** 404 NOT FOUND

#### ❌ Тест 4.9: Обновление товара, которого нет в корзине

**Запрос:**
- **Метод:** PUT
- **URL:** `http://localhost:8000/api/cart/update`
- **Body:**
```json
{
  "product_id": 5,
  "quantity": 2,
  "cart": {
    "1": 2
  }
}
```

**Ожидаемый результат:**
```json
{
  "detail": "Product with id 5"
}
```

**Статус код:** 404 NOT FOUND

#### ❌ Тест 4.10: Удаление товара, которого нет в корзине

**Запрос:**
- **Метод:** DELETE
- **URL:** `http://localhost:8000/api/cart/remove/5`
- **Body:**
```json
{
  "cart": {
    "1": 2
  }
}
```

**Ожидаемый результат:**
```json
{
  "detail": "Product with id 5 not found in cart"
}
```

**Статус код:** 404 NOT FOUND

---

## 🔄 Полный сценарий покупки (E2E тест)

### Шаг 1: Просмотр всех категорий
`GET /api/categories` → получаем список категорий

### Шаг 2: Выбор категории
`GET /api/products/category/1` → смотрим товары в Electronics

### Шаг 3: Просмотр деталей товара
`GET /api/products/1` → смотрим детали Wireless Headphones

### Шаг 4: Добавление в корзину
`POST /api/cart/add` → добавляем 2 наушника

### Шаг 5: Продолжение покупок
`GET /api/products/category/3` → смотрим Books

### Шаг 6: Добавление еще товара
`POST /api/cart/add` → добавляем книгу (product_id: 7)

### Шаг 7: Проверка корзины
`POST /api/cart` → смотрим общую стоимость

### Шаг 8: Изменение количества
`PUT /api/cart/update` → меняем количество наушников на 1

### Шаг 9: Финальная проверка
`POST /api/cart` → проверяем обновленную сумму

---

## 💡 Советы по тестированию

### Переменные окружения в Postman

Создайте переменные для удобства:
- `base_url` = `http://localhost:8000`
- `product_id` = `1` (можно менять)
- `category_id` = `1` (можно менять)

Использование: `{{base_url}}/api/products/{{product_id}}`

### Проверка результатов

После каждого запроса проверяйте:
1. **Status Code** - соответствует ожидаемому
2. **Response Time** - разумное время ответа (< 500ms для локального сервера)
3. **Response Body** - структура и данные корректны
4. **Headers** - присутствует `Content-Type: application/json`

### Автоматизация тестов

В Postman можно добавить тесты во вкладке "Tests":

```javascript
// Проверка статус кода
pm.test("Status code is 200", function () {
    pm.response.to.have.status(200);
});

// Проверка наличия поля
pm.test("Response has products array", function () {
    var jsonData = pm.response.json();
    pm.expect(jsonData).to.have.property('products');
});

// Проверка значения
pm.test("Total is greater than 0", function () {
    var jsonData = pm.response.json();
    pm.expect(jsonData.total).to.be.above(0);
});
```

---

## 🐛 Типичные ошибки и решения

### Ошибка: Connection refused
**Причина:** Сервер не запущен
**Решение:** Запустите `python backend/run.py`

### Ошибка: 404 для всех запросов
**Причина:** Неправильный URL
**Решение:** Проверьте, что используется `http://localhost:8000`

### Ошибка: База данных пуста
**Причина:** Не запущен seed_data.py
**Решение:** Выполните `python backend/seed_data.py`

### Ошибка: CORS
**Причина:** Если тестируете из веб-приложения
**Решение:** CORS настроен в config.py, добавьте нужный origin

---

## 📊 Чек-лист тестирования

- [ ] Все GET запросы возвращают данные
- [ ] Все POST запросы корректно добавляют данные
- [ ] PUT запросы обновляют данные
- [ ] DELETE запросы удаляют данные
- [ ] Ошибки 404 возвращаются для несуществующих ресурсов
- [ ] Корзина корректно считает суммы
- [ ] Корзина корректно обрабатывает пустое состояние
- [ ] Все операции с корзиной работают последовательно
- [ ] Связь товаров и категорий работает корректно

---

## 🎓 Дополнительные упражнения

1. **Граничные значения:** Попробуйте добавить товар с quantity: 0, -1, 1000000
2. **Последовательность операций:** Добавьте товар, удалите его, попробуйте обновить
3. **Производительность:** Проверьте время ответа при большой корзине (100 товаров)
4. **Одновременные запросы:** Отправьте несколько запросов параллельно

Удачного тестирования! 🚀